https://codepen.io/Clare-Jackson/pen/jOqPbVm?editors=1100

  1. What is Semantic HTML? 
The tags that clearly define the information they contain

    2. What is HTML used for? 
The language read by browsers to display webpages

    3. What is an attribute and where do we put it? 
An attribute defines a property or reference of a certain HTML tag

    4. What is the h1 tag used for? How many times should I use it on a page?
h1 is used for the most important heading on a page and it should be used one time only

    5. Name two tags that have required attributes
The <a> and <img> tags require attributes

    6. What do we put in the head of our HTML document? 
The information about that page which is unseen, but read by the browser

    7. What is an id? 
An id is a tag labeling a special CSS design instruction

    8. What elements can I add an id to? 
Any elements, but one at a time for each id tag

    9. How many times can I use the same id on a page? 
Once

    10. What is a class? 
A class is a tag that labels a special CSS design instruction

    11. What elements can I add a class to? 
They can include any elements

    12. How many times can I use the same class on a page? 
As many times as you want

    13. How do I get my link to open in a new tab?
Hold down the Cmd button while clicking the link

    14. What is the alt attribute in the image tag used for? 
It is used by screen readers for the impaired user to know what the picture contains without actually seeing it

    15. How do I reference an id?
To reference an id in CSS, use the # character 

    16. What is the difference between a section and a div
Section is a semantic tag as it conveys meaning and div may be applied for a wide range of purposes for the developer

    17. What is CSS used for? 
CSS is used for styling HTML 

    18. How to we select an element? Example - every h2 on the page
h2 {CSS design: specifics;}

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
A class is CSS style being applied to multiple elements and id is style applied only to one. A class may be used for all of the images on a page. An id may be applied to some contact info on a website.

    20. How do we select classes in CSS?
Classes are selected using . then the class name from the “” without a space

    21. How do we select a p element with a single class of “human””?
P .human {}

    22. What is a parent child selector? When would this be useful? 
The parent child selector is a way of coding that points to the element inside of a main element. The parent is the main and the child elements are contained inside. This is useful for large amounts of code to point the way for a developer changing any coding or design.

    23. How do you select all links within a div with the class of sidebar?
.divsidebar a {}

    24. What is a pseudo selector?
It is for elements in a special state, such as links already clicked, hovers, etc.

    25. What do we use the change the spacing between lines?
Use "line-height"

    26. What do we use to change the spacing between letters?
Use "letter-spacing"

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
Use "text-transform: uppercase;". Use "text-transform: lowercase;". Use "text-transform: capitalize;".
    28. How do I add a 1px border around my div that is dotted and black?
div {border: 1px dotted black}

    29. How do I select everything on the page? 
Use the universal selector * or html

    30. How do I write a comment in CSS?
Use the /* comment */ to write a comment in CSS

    31. How do I find out what file I am in, when I am using the command line? 
The file should be in the prompt line in the terminal.

    32. Using the command line - how do I see a list of files/folders in my current folder? 
Type "ls" to list the files/folders/directories

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
Type "rm (file name)". This removes without any warning and for good.

    34. Why should I use version control? 
It will be helpful when working with a team of developers and can track all of the changes individually, in order.

    35. How often should I commit to github?
Whenever I am finished with a new feature and need to make a note of it 

    36. What is the command we would use to push our repo up to github? 
Git puch -u origin Clare-Jackson

    37. Walk me through Lambda's git flow. 
Fork your repo
Add TL in Settings, Manage Access, Invite a Collaborator
Clone the repo by clicking the copy to clipboard button
Move into the directory you want to hold the new cloned repo
Type git clone then paste the repo link
Cd into new repo
Type git checkout -b "Clare-Jackson"
Work on the repo

Stretch Questions

    1. What is the difference between an inline element and a block element?
    2. What happens when an element is positioned absolutely? 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 